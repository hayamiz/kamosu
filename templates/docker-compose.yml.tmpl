services:
  kb:
    image: ghcr.io/goda-lab/kamosu:${KB_TOOLKIT_VERSION:-latest}
    volumes:
      - .:/workspace
    env_file:
      - .env
    working_dir: /workspace
    stdin_open: true
    tty: true
