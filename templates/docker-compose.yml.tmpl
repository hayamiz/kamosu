services:
  kb:
    image: hayamiz/kamosu:${KB_TOOLKIT_VERSION:-latest}
    volumes:
      - .:/workspace
    env_file:
      - .env
    environment:
      - HOST_UID=${HOST_UID:-0}
      - HOST_GID=${HOST_GID:-0}
    working_dir: /workspace
    stdin_open: true
    tty: true
